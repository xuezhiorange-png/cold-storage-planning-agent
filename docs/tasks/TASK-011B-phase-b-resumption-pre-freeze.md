# Task 11 Phase B Resumption — Pre-Freeze Design Contract

**Status:** DESIGN-ONLY / DRAFT / awaiting Charles freeze authorization
**Created:** 2026-07-08 (server UTC)
**Author:** Hermes (proposal subject to Charles-authorized freeze review)
**Branch base:** `main @ 1f6289ad37464b7f5effea4e3d3cd059e4a22751`
**Branch name (proposed):** `codex/task-11b-phase-b-resumption-from-main`
**Target Phase:** Task 11 Phase B (resumption after Issue #35 close)
**Authoritative references:**
- `docs/tasks/TASK-011-evaluation-pilot-readiness.md` (PR #21's design doc — Round 12 addendum)
- `docs/tasks/TASK-011B-phase4-issue35-production-roundtrip-governance.md` (Phase 4 governance, §16 / §17 / §18 / §19 / §20)
- Issue #35 (closed 2026-07-08, state_reason=completed via the §16 #15 defer-record comment `4911628412`)
- PR #21 (Draft / Open / Not merged / head `7822581eeee4c590b4ed9b1e3c46c1cde5490098`)
- PR #46 (merged at `c650619efb65411e1da04fbd9387c34fb6dcd5fc`, Slice 2C)
- PR #47 (merged at `1f6289ad37464b7f5effea4e3d3cd059e4a22751`, Slice 2D)
- post-merge main CI run `28918071267` (completed / success / 4 jobs)

---

## 1. Decision (proposed for Charles freeze review)

### 1.1 PR #21

**Decision:** PR #21 should be marked **Superseded** and remain Draft / Open / Not merged. PR #21's branch ref `codex/task-11-evaluation` is **not** rebased, force-pushed, merged, or touched in this round.

Rationale: PR #21's evaluation module is **frozen-by-design** with a hard always-raise gate (`_require_scheme_production_prerequisite`) + ~12 frozen test assertions (`outcome == "blocked"`) + 1249 + 1242 LOC reviewer-frozen expected outputs (`expected/baseline-feasible.v1.json`, `expected/high-throughput-review.v1.json`). Mixing in-place refresh with rejected Round 11 directions + Phase-4 main rewires would re-introduce the very fabrications the Round 11 review rejected. PR #21 is therefore a historical artefact, not a workable starting point for Phase B.

### 1.2 Phase B Resumption branch

**Decision:** Phase B Resumption should be implemented as a **new branch from current `main`**, named:

```
codex/task-11b-phase-b-resumption-from-main
```

with base SHA `1f6289ad37464b7f5effea4e3d3cd059e4a22751` (= current `main` HEAD post-Issue-#35 close).

The resumption PR's body must explicitly state "PR #21 superseded by this PR — see `docs/tasks/TASK-011B-phase-b-resumption-pre-freeze.md` §1.1". This contract (§1.1 and the full document) is the contract that supersedes PR #21; the resumption PR is the vehicle that closes out Phase B once Charles authorizes it.

### 1.3 Evaluation contract refresh

**Decision:** Evaluation contract transitions from **"blocked-by-design"** → **"real production path success"** in the new resumption PR. Three coupled refreshes are required:

1. **`backend/src/cold_storage/evaluation/execute.py`** — remove (or conditionalize) the always-raise `_require_scheme_production_prerequisite()` gate. The schemes-stage call must invoke `compose_production_scheme_service(session_factory)` and persist `SchemeRun` through production SchemeService.
2. **`backend/src/cold_storage/evaluation/errors.py`** — either remove `EvaluationPrerequisiteMissingError` entirely (since the gap it declares is closed), or migrate it to a *deprecation* contract that records the historical Round 12 block and remains a side-channel diagnostic without being raised on the happy path.
3. **`backend/tests/evaluation/test_sqlite_acceptance.py`** + companion `test_postgresql_acceptance.py` (to be added or extended in the resumption PR) — flip the 12 frozen "blocked" assertions to "success" assertions, and add new tests asserting production-path provenance (every SchemeRun row records its `calculation_run_id` lineage, every SourceBinding's `combined_source_hash` is verifiable, the orchestrator's `attempt_status` survives failure injection, etc.). Tests MUST assert no `EVAL_PRODUCTION_PIPELINE_PREREQUISITE_MISSING` is raised on the happy path.

### 1.4 Expected outputs regeneration

**Decision:** `evaluation/expected/baseline-feasible.v1.json` and `evaluation/expected/high-throughput-review.v1.json` MUST be **re-generated by running the new production path**, not by any evaluation-owned calculation. The regeneration must:

1. Run the baseline / high-throughput scenarios end-to-end against a fresh SQLite database (and a separate fresh PostgreSQL database) on the resumption branch.
2. Use **approved non-demo coefficients** (Phase-4 governance path: `production_source_binding.py` + `application/ports.py` + `infrastructure.repositories.SqlAlchemyProductionIdentityRepository`).
3. Capture the canonicalized output of the real production SchemeService invocation. The captured output MUST be Charles-reviewed before any expected-output file is committed.
4. `evaluation/expected/invalid-blocked.v1.json` does not require regeneration (its scenario never reaches the schemes stage; it is upstream of the gate and is unaffected by Phase-4 changes).

The expected-output regeneration is a separate freeze step that requires Charles's review sign-off analogous to the Round 8 frozen-contract review.

### 1.5 Forbidden paths (frozen in this contract)

The following are **explicitly forbidden** in any future implementation round under this contract:

- **DO NOT** restore `backend/src/cold_storage/evaluation/production_seeding.py` (Round 11 deletion; rejection reason: evaluation-owned production row fabrication, engineering-input bridges for cooling_load / equipment).
- **DO NOT** write `CalculationRunRecord`, `SourceBindingRecord`, orchestration identity / attempt / execution-snapshot / coefficient-context / approved weight-set revision rows from evaluation code. Production rows MUST be persisted by `compose_production_scheme_service(session_factory)` (Phase-4 main entrypoint).
- **DO NOT** hand-write production snapshots in evaluation code.
- **DO NOT** synthesize engineering inputs in evaluation code (cooling_load / equipment input bridges from upstream stage outputs are forbidden).
- **DO NOT** use raw ORM `Session.add(...)` + `session.flush()` from evaluation code to fabricate rows that production path is supposed to produce.
- **DO NOT** introduce demo / latest-row / partial-binding fallbacks into the production path.
- **DO NOT** suppress, rename, downgrade, or reclassify `requires_review` warnings on coefficients.
- **DO NOT** alter production formulas, coefficient values, scoring rules, review rules, thresholds, weights, or migrations.
- **DO NOT** bypass `SourceBindingVerifier` or `SchemeService`.
- **DO NOT** bypass approved non-demo coefficient governance.
- **DO NOT** rebase, force-push, or merge PR #21 from this contract or any downstream round.

---

## 2. Why PR #21 cannot be refreshed in-place

### 2.1 Topology

- PR #21 head SHA `7822581eeee4c590b4ed9b1e3c46c1cde5490098` is **20 commits ahead** of `origin/main` and **33 commits behind** it (post-PR #47 merge).
- PR #21's merge-base with `origin/main` is `e6dcd631059d1106947ff947ef8c5b9e1e214035`.
- 33 commits on main that PR #21 doesn't have include: Task 011B Phase 1 schema/identity foundation (PR #37); Phase 2 ports/adapters (PR #38); Phase 3 sourcebinding-schemeservice-e2e (PR #41); Phase 4 design contract (PR #43); Slice 1 governance (`6efa29b`); Slice 2A (`f7a5c28`); Slice 2C (`c650619` = PR #46); Slice 2D (`1f6289ad` = PR #47); plus a series of `database_backend` threading fixes through the SchemeService write path.
- The conflict surface is narrow textually (PR #21 touches `service.py` for `sorted(_REQUIRED_CALC_TYPES)`; Phase-1/4 also touches `service.py`). Semantically wider, because Phase-1/4 re-wires the same ports that PR #21's evaluation module depends on.

### 2.2 Frozen-by-test contract

`backend/tests/evaluation/test_sqlite_acceptance.py` (1066 LOC) contains **at least** these tests that assert the runner is blocked:

- `test_baseline_run_fails_blocked`
- `test_baseline_outcome_blocked_by_prerequisite`
- `test_baseline_manifest_declares_success`
- `test_high_throughput_run_fails_blocked`
- `test_high_throughput_outcome_blocked`
- `test_full_suite_fails_blocked`
- `test_prerequisite_blocker_error_class_exists`
- `test_prerequisite_blocker_no_fake_records`
- `test_prerequisite_blocker_no_dev_db_touch`
- `test_structured_blocker_in_raw_artifact`

Each test is a contract on `outcome == "blocked"` or specific `details` in `EvaluationPrerequisiteMissingError`. Flipping these requires Charles's freeze sign-off, which is what this design document obtains.

### 2.3 Frozen-by-reviewer expected outputs

`evaluation/expected/baseline-feasible.v1.json` (1249 LOC) and `evaluation/expected/high-throughput-review.v1.json` (1242 LOC) are **reviewer-frozen** reviewer-approval records from Round 8. They were authored against the **pre-Phase-4 production path**. Phase-4 main rewires approved-coefficient resolution + SchemeService schema-snapshot persistence; the numeric outputs of the new path will **necessarily** differ. Even if PR #21's gate were lifted, these expected files would not match the new outputs. They cannot be co-opted into a Phase-4 path without invalidating their reviewer-frozen status.

### 2.4 Narrative stale across multiple files

PR #21's narrative staleness appears in **at least 6 places**:

- `docs/tasks/TASK-011-evaluation-pilot-readiness.md` §15 ("Phase B is blocked. See Issue #22").
- `docs/tasks/TASK-011-evaluation-pilot-readiness.md` "Status:" line ("standalone follow-up after Issue #22 closure").
- `docs/tasks/TASK-011-evaluation-pilot-readiness.md` "Prerequisite Issue:" line.
- `backend/src/cold_storage/evaluation/errors.py` `EvaluationPrerequisiteMissingError` docstring.
- `backend/src/cold_storage/evaluation/execute.py` `_require_scheme_production_prerequisite` comment block.
- PR #21 squash commit message (immutable, but referenced from any reviewer walking the commit history).

Issue #22 closed TransportB E2E persistence (a different gap). Issue #35 (Phase-4) closed the standalone capability gap PR #21 was waiting on. **Issue #35 closes the production-side prerequisite for PR #21 but does NOT close PR #21's evaluation-side contract.** The two are independent close conditions.

### 2.5 Re-introducing the rejection risk

PR #21's own squash commit message (Round 12 reversal) explicitly states:

> "Round 11 attempted to drive the production SchemeService via an evaluation-owned `production_seeding` module (1.2k LoC). The independent engineering review rejected that direction because it fabricates production records in evaluation code (raw ORM inserts of CalculationRunRecord, SourceBindingRecord, orchestration identity/attempt/execution-snapshot/coefficient-context rows, and approved weight-set revisions) plus evaluation-owned engineering-input bridges for cooling_load/equipment. Round 12 reverses that direction in full."

Any in-place refresh of PR #21 risks **silently re-introducing** the same fabrication pattern under the new branch topology, even if the git history looks clean. The Round 12 rejection was an independent engineering review verdict; rebasing PR #21 forward requires implicitly overriding that verdict without re-running review.

### 2.6 Conclusion

**PR #21 is a frozen-by-design blocked contract**, not a working draft. It must be either:

(a) marked **Superseded** (this contract's recommendation) and left untouched, with Phase B Resumption opening a new branch; or
(b) re-authored as a separately-named branch from main with the full Round-12-followup review sign-off cycle re-run.

This contract recommends (a).

---

## 3. New resumption branch strategy

### 3.1 Branch identity

```
local:    codex/task-11b-phase-b-resumption-from-main
remote:   origin/codex/task-11b-phase-b-resumption-from-main
base:     main @ 1f6289ad37464b7f5effea4e3d3cd059e4a22751
PR title: feat(task-11): Phase B resumption after Issue #35 close
```

### 3.2 Branch policy

- **First commit (pre-freeze round)**: this design contract document (`docs/tasks/TASK-011B-phase-b-resumption-pre-freeze.md`).
- **Second commit (implementation rounds, future-authorized)**: implementation per §5-§7.
- **No commit** in this branch may modify `docs/tasks/TASK-011-evaluation-pilot-readiness.md` body text (PR #21's design doc); updates to that doc belong in PR #21 itself if Charles later decides to do so, or in a separate "TASK-011 narrative refresh" round. Until then, PR #21's doc remains an immutable historical record.
- **No commit** may modify `production_seeding.py` (file remains absent, deletion is irreversible).

### 3.3 PR body required fields (for the resumption PR)

When the resumption PR is opened, its body MUST contain:

1. Base SHA: `1f6289ad37464b7f5effea4e3d3cd059e4a22751` (= main HEAD at branch-time).
2. Reference to this contract: `docs/tasks/TASK-011B-phase-b-resumption-pre-freeze.md`.
3. Reference to PR #21 being superseded: `supersedes #21` in PR description.
4. Statement of `expected_outcome=success` for the baseline and high-throughput scenarios on both backends.
5. Statement of `EVAL_PRODUCTION_PIPELINE_PREREQUISITE_MISSING` no longer being raised on the happy path.
6. Statement that expected outputs were regenerated from real production path, with reviewer sign-off recorded.
7. Reference to the Phase-4 governance contract: `docs/tasks/TASK-011B-phase4-issue35-production-roundtrip-governance.md` §15 (architecture boundaries) + §16 (acceptance criteria closed by Issue #35 close).

---

## 4. Allowed future implementation files

The following files are candidate modification targets for the resumption implementation round(s). They are listed here for **frozen-scope** purposes; the actual modification of any file is a future round subject to its own authorization.

### 4.1 Evaluation subsystem

| file | path | allowed kind of modification |
|---|---|---|
| Manifest | `evaluation/manifest.json` | `expected_outcome` already `success`; no required change unless baseline/high-throughput fixture revisions need updating |
| Expected outputs | `evaluation/expected/baseline-feasible.v1.json` | Regenerate under reviewer sign-off (matches new production-path numeric values); do **not** quietly rewrite |
| Expected outputs | `evaluation/expected/high-throughput-review.v1.json` | Regenerate under reviewer sign-off |
| Expected outputs | `evaluation/expected/invalid-blocked.v1.json` | No change anticipated (unaffected by Phase-4) |
| Runner | `backend/src/cold_storage/evaluation/execute.py` | Lift the always-raise gate; invoke `compose_production_scheme_service(session_factory)` and persist SchemeRun via production SchemeService |
| Runner | `backend/src/cold_storage/evaluation/cli.py` | No changes anticipated unless CLI surface needs new flags (e.g., `--prerequisite-tolerate`) |
| Runner | `backend/src/cold_storage/evaluation/run_directory.py` | No changes anticipated |
| Errors | `backend/src/cold_storage/evaluation/errors.py` | Migrate `EvaluationPrerequisiteMissingError` to a documented historical contract; or remove if no caller remains |
| Adapter | `backend/src/cold_storage/evaluation/` (new module, optional) | New evaluation-to-orchestration adapter MAY be added if it calls ONLY `compose_production_scheme_service(session_factory)` and persists NO production rows directly. The adapter's contract: receive `EvaluationScenario` + a fresh `Session`; delegate scheme execution to the production service; surface the resulting `SchemeRun` row to evaluation's stage ledger. Adapter **MUST NOT** fabricate, bypass, or short-circuit SchemeService. |

### 4.2 Test code

| file | path | allowed kind of modification |
|---|---|---|
| SQLite acceptance | `backend/tests/evaluation/test_sqlite_acceptance.py` | Flip frozen "blocked" assertions to "success"; add new provenance verification; remove or clearly distinguish deprecated-blocker tests |
| SQLite acceptance | `backend/tests/evaluation/test_cli.py` | Update assertions that depend on exit-code-on-block to exit-code-on-success where applicable |
| Acceptance consistency | `backend/tests/evaluation/test_fixture_consistency.py` | No changes anticipated unless fixture schema evolved |
| PostgreSQL acceptance | `backend/tests/evaluation/test_postgresql_acceptance.py` | **Add** if missing; mirror `test_sqlite_acceptance.py` with PostgreSQL backend; assert no demo / latest-row / partial-binding paths exist |
| Architecture | `backend/tests/architecture/test_architecture_boundaries.py` | No changes anticipated unless Phase-4 added new forbidden-import rules |
| Identity / SourceBinding | `backend/tests/integration/test_production_archive_wiring_e2e_postgresql.py` + zero-delta tests | No changes anticipated (PR #47 baseline; may be referenced from resumption tests) |

### 4.3 Fixtures

| file | path | allowed kind of modification |
|---|---|---|
| Project fixture | `evaluation/fixtures/projects/baseline-feasible.v1.json` | May be updated with a `coefficient_revision_id` pointer to the approved non-demo coefficient revision used in Phase-4 main; coordinate with fixture-consistency tests |
| Project fixture | `evaluation/fixtures/projects/high-throughput-review.v1.json` | Same |
| Project fixture | `evaluation/fixtures/projects/invalid-blocked.v1.json` | No change anticipated |

### 4.4 Documentation

| file | path | allowed kind of modification |
|---|---|---|
| Pilot runbook | `docs/pilot/TASK-011-PILOT-RUNBOOK.md` | May need updates if `evaluation/run` CLI surface changes |
| This contract | `docs/tasks/TASK-011B-phase-b-resumption-pre-freeze.md` | Frozen after Charles sign-off; further changes require a new "amendment" round |

### 4.5 What MUST NOT be touched

- `backend/src/cold_storage/evaluation/production_seeding.py` — file remains absent. Restoration is a stop condition (§8).
- All `backend/src/cold_storage/modules/*/infrastructure/*.py` production code — Phase-4 has its own freeze; any modification must be approved as a new Phase-5.
- All `backend/src/cold_storage/modules/*/application/*.py` ports — Phase-2 + Phase-4 freeze.
- All Alembic migrations under `backend/src/cold_storage/modules/*/infrastructure/migrations/versions/*.py` — Phase 1/2/3/4 freeze.
- All approved coefficient resolution code (`backend/src/cold_storage/modules/orchestration/application/ports.py` + `infrastructure/repositories.py`) — frozen by PR #46.

---

## 5. Forbidden future implementation paths (frozen in this contract)

The following are **explicitly forbidden** in any future implementation round. Each entry is load-bearing — listing the forbidden pattern here is what allows the resumption PR to be reviewed without re-litigating the Round 11 reversal.

### 5.1 Round 11 reversal — DO NOT restore

- **DO NOT** re-introduce `backend/src/cold_storage/evaluation/production_seeding.py`. The 1.2k LoC evaluation-owned production row fabrication that the Round 11 independent engineering review rejected. Restoration IS itself a §8 stop condition.

### 5.2 Evaluation-owned production row fabrication — DO NOT

- **DO NOT** create or use any module that writes `CalculationRunRecord` from `cold_storage.evaluation.*` code, regardless of mechanism (raw `Session.add`, `session.flush`, `bulk_insert_mappings`, manual SQL, or any other ORM-style technique).
- **DO NOT** create or use any module that writes `SourceBindingRecord` from evaluation code.
- **DO NOT** create or use any module that writes orchestration identity / attempt / execution-snapshot / coefficient-context rows from evaluation code.
- **DO NOT** create or use any module that writes approved weight-set revision rows from evaluation code.
- **DO NOT** engineer evaluation-owned calculation input bridges (e.g., `cooling_load` / `equipment` inputs derived from upstream stage outputs in evaluation code).

### 5.3 Production invariants — DO NOT weaken

- **DO NOT** introduce demo / unverified coefficients into the production path (they are reserved for harness-only use, marked `requires_review=true`).
- **DO NOT** introduce "latest-row" selection on approved coefficient queries.
- **DO NOT** introduce partial `SourceBinding` writes that succeed despite missing required slots.
- **DO NOT** suppress, rename, downgrade, or reclassify `requires_review` warnings or `UntrustedCoefficientError` raise paths.
- **DO NOT** alter production formulas (the cooling-load formula, coefficient values, scoring rules, review rules, thresholds, weights).
- **DO NOT** bypass `SourceBindingVerifier` or `SchemeService`.
- **DO NOT** bypass approved non-demo coefficient governance.

### 5.4 PR #21 — DO NOT touch

- **DO NOT** rebase, force-push, merge, comment on, label, or otherwise mutate PR #21 in any resumption round. PR #21 is historical; manipulating it risks damaging the audit history of the Round 11 reversal.
- **DO NOT** modify PR #21's local branch `codex/task-11-evaluation` from the resumption branch.

### 5.5 Demonstration / fixture boundary — DO NOT blur

- **DO NOT** import evaluation code into production modules.
- **DO NOT** import production modules into evaluation code EXCEPT through their public ports (`application/ports.py` / `compose_production_scheme_service(session_factory)`).
- **DO NOT** bypass `application/ports.py` to reach `infrastructure/orm.py` from evaluation code.

---

## 6. Required success semantics for Phase B Resumption

When the resumption PR is merged, the following invariants MUST hold for the **baseline-feasible.v1** scenario (and analogously for **high-throughput-review.v1**):

1. `compose_production_scheme_service(session_factory)` is invoked exactly once per scenario through `backend/src/cold_storage/evaluation/execute.py`. It is the **only** path that writes production rows for that scenario.
2. `SchemeRun.scheme_status == "SUCCEEDED"` at the end of the scenario.
3. The `evaluation/raw/<scenario_id>.json` artifact's top-level `outcome == "success"` (not `"blocked"`, not `"failed"`).
4. No `EVAL_PRODUCTION_PIPELINE_PREREQUISITE_MISSING` error is raised on the happy path. (The error class MAY exist as a deprecated diagnostic; it MUST NOT be raised.)
5. The SchemeRun row carries a verifiable `calculation_run_id` lineage back to all 5 production `CalculationRunRecord` rows (zone, investment, cooling_load, equipment, plus the planning row counted by Phase-4 design §4.3). PK-set symmetric-difference test passes (one row per table before, one row per table after, delta = ∅ across all four production tables).
6. The `combined_source_hash` on the scheme's `SourceBindingRecord` is reproducible from `build_source_snapshot_content_v1(...)` and round-trips through the scheme-callable path.
7. The expected output comparison (`evaluation/expected/baseline-feasible.v1.json`) passes with all exact-paths + decimal-paths matching and all ignored-paths justified.
8. The same invariants hold on PostgreSQL backend as on SQLite, with migration `alembic upgrade head` applied first.

---

## 7. Minimal validation matrix for the future implementation PR

The future resumption PR MUST validate the following before being marked Ready:

### 7.1 Backend validation

- **`ruff check src tests`** clean (no new warnings).
- **`ruff format --check src tests`** clean.
- **`mypy src`** clean (no new errors; pre-existing errors are out-of-scope unless annotated as such).
- **SQLite evaluation acceptance suite**:
  - `backend/tests/evaluation/test_sqlite_acceptance.py` — all tests green, including flipped "success" assertions.
- **PostgreSQL evaluation acceptance suite** (newly added or extended):
  - `backend/tests/evaluation/test_postgresql_acceptance.py` — mirror of SQLite suite, on a fresh PostgreSQL 14 instance with `alembic upgrade head` applied.
- **Phase-4 regression suite**:
  - `backend/tests/integration/test_production_archive_wiring_e2e_postgresql.py` — pass (PR #47 baseline).
  - `backend/tests/integration/test_zero_delta_invariant_postgresql.py` — pass (PR #47 baseline).
  - `backend/tests/integration/test_zero_delta_invariant_sqlite.py` — pass (PR #47 baseline).
  - `backend/tests/integration/test_phase4_slice2a_resolution_gateway_postgresql.py` — pass (PR #46 baseline).
  - `backend/tests/integration/test_phase4_slice2c_power_authority_postgresql.py` — pass (PR #46 baseline).
- **Architecture tests**:
  - `backend/tests/architecture/test_architecture_boundaries.py` — pass (no application file imports from `infrastructure/orm.py`; `phase3_exceptions` exception set remains retired).
- **Production flow failure-closed tests** (§11 + §13 + §12 + §14):
  - 9 fail-closed test cases from `TASK-011B-phase4-issue35-production-roundtrip-governance.md` §11 — all green on both backends.
  - §12 power-authority test — green on both backends.
  - §13 archive verification test — green on both backends.
  - §14 phase3 retirement verification — green (no application file imports from `infrastructure/orm.py`; `phase3_exceptions` exception set absent).

### 7.2 Expected-output validation

- `evaluation/expected/baseline-feasible.v1.json` regenerated under reviewer sign-off, with the regenerated file matching the canonicalized output of the production path on a fresh SQLite database. The sign-off record lives in the resumption PR's commit message / merge commit body / or a designated `docs/tasks/TASK-011B-phase-b-resumption-expected-outputs-review.md` (separate-document pattern allowed).
- `evaluation/expected/high-throughput-review.v1.json` regenerated with the same discipline.
- `evaluation/expected/invalid-blocked.v1.json` unchanged (or changed only if the schema evolved, with reviewer sign-off).

### 7.3 CI validation

- 4 jobs on PR-side run green: compose-config / frontend / backend-sqlite / backend-postgresql.
- Post-merge run on main, head_sha == resumption PR's merge commit SHA, completed / success / 4 jobs all success.
- backend-postgresql job's steps include success of: `PostgreSQL integration tests` / `PostgreSQL attempt race stability (10x)` / `Backend tests (PostgreSQL)`.
- No `workflow_dispatch` or rerun after the merge.

### 7.4 Post-conditions

- main HEAD == resumption PR's merge commit SHA.
- `codex/task-11b-phase-b-resumption-from-main` ref deleted (or retained per Charles's choice).
- PR #21 retains its `Draft / Open / Not merged` state, with a `superseded by #N` cross-reference comment posted on PR #21 by Charles (or in PR #21's body under "Superseded by" trailer) — this is an out-of-band Charles action, not bound to the resumption PR.

---

## 8. Stop conditions

The following conditions halt the Phase B Resumption immediately. Any one trigger = STOP, surface evidence, do not continue.

1. `production_seeding.py` (or any equivalent) is found reintroduced in evaluation code.
2. Any raw `Session.add` / `session.flush` of `CalculationRunRecord` / `SourceBindingRecord` / orchestration identity / attempt / execution-snapshot / coefficient-context / approved weight-set revision rows is found in evaluation code paths.
3. Any demo / latest-row / partial-binding fallback enters the production path.
4. Any `requires_review` warning is suppressed, renamed, or downgraded.
5. Production formulas, coefficient values, scoring rules, review rules, thresholds, or weights are altered.
6. `compose_production_scheme_service(session_factory)` is bypassed or short-circuited.
7. `SourceBindingVerifier` or `SchemeService` is bypassed.
8. Approved non-demo coefficient governance is bypassed (e.g., a code path that selects a non-approved row for production).
9. PR #21 is rebased, force-pushed, merged, or otherwise mutated.
10. The expected-output regeneration is committed without Charles's reviewer sign-off recorded.
11. CI reruns are performed that mask real failures.
12. `evaluation/manifest.json` `expected_outcome` is downgraded from `"success"` to `"blocked"` (or any value other than `"success"`).
13. Any new migration is added under `backend/src/cold_storage/modules/*/infrastructure/migrations/versions/*.py` without following the Phase-4 design contract's migration discipline.

Each stop is reported with: condition number, evidence (file path, test name, log snippet, or commit SHA), and a recommendation for next steps. Format aligned with `TASK-011B-phase4-issue35-production-roundtrip-governance.md` §20.

---

## 9. Governance

- **PR #21** remains **OPEN / Draft / untouched** in this round. No rebase. No merge. No body edit. No comments posted.
- **Issue #35** remains **CLOSED** (its close and state_reason=completed are accepted as facts of record).
- **Issue #20** (Task 11 evaluation): no mutation this round.
- **Task 11 Phase B** remains **NOT resumed** in this round. No implementation begins until Charles authorizes a future round against this frozen contract.
- **Task 11 Phase C / Phase D / Task 12**: each will require its own design freeze and explicit authorization per `TASK-011B-phase4-issue35-production-roundtrip-governance.md` §19 deferred items.
- This document is a **design contract only**. It is not a Ready / merge authorization for any PR.
- After Charles freezes this contract, this document becomes the §15 / §17 reference for the future Phase B Resumption PR.

---

## 10. Change log

- 2026-07-08 (this commit): initial pre-freeze draft, awaiting Charles freeze review. No implementation begins until Charles signs off.
