# V1.1 会话交接（2026-08-29）

**给谁：** Charles，以及下一会话的 coding agent。  
**为什么写：** V1.1 已打标签发版，需要一份可独立打开的现状。  
**权威：** 代码与 Git 标签以 `origin/main` 为准。本文不替代合同、ADR、或计算器源码。

新会话第一步：读本文 + `AGENTS.md`。不要用 `docs/audit/current-state.md` 的 §1–30 当当前能力（那是 Task 0 冻结快照）。V1.0 冻结交接仍在 `docs/handover/2026-08-28-v1.0.md`。

---

## 1. 当前身份

| 项 | 值 |
|---|---|
| 仓库 | https://github.com/xuezhiorange-png/cold-storage-planning-agent |
| 产品标签 | **`v1.1.0`**（Latest） |
| Release | https://github.com/xuezhiorange-png/cold-storage-planning-agent/releases/tag/v1.1.0 |
| Tag 对象 | `31079ea0c655f9d3b804c45cdf7ef453eae7a495` |
| Tag 指向 | `7fd0a28659baca56570813f3380b8223a0114f57` |
| 该 SHA 说明 | `feat(aily): 飞书 MCP 改走 Streamable HTTP 完整 JSON (#243)` |
| 该 SHA 主干 CI | 绿 https://github.com/xuezhiorange-png/cold-storage-planning-agent/actions/runs/33240472277 |
| 上一标签 | `v1.0.0` → `590b53f40d520db51680e4132ff5ef67fd2995ce`（**禁止移动**） |
| 再上一标签 | `v0.9.0`（**禁止移动**） |
| 操作者 | Charles（学致 程 / `xuezhiorange@gmail.com`） |
| 对操作者说话 | 中文 |

`backend/pyproject.toml` 与 `frontend/package.json` 仍写 `0.1.0`。那是实现遗留，**不是**产品版本。产品版本看 Git 标签。

---

## 2. 产品是什么 / 不是什么

这是蓝莓等农产品加工的**冷库规划 / 概念设计助手**，加上豆包工作伙伴对话入口。

**不是：** 设计院替代、注册工程师成果、消防/结构/电气施工/压力管道设计、最终设备选型、施工图、现场设备控制。

大模型**不得**直接算工程量。缺关键参数必须显式报错，禁止默默猜。Demo 系数永远 `source_type=demo`、`validity_status=unverified`、`requires_review=true`。

`AILY_OUTBOUND_LIVE_SESSION=NO`。不要从本仓库打开飞书出站会话。不要声称生产 RBAC。`mark_reviewed` / `approve` 不是 MCP 工具。

---

## 3. Charles 的站立指令

1. **红了就自己修，绿了就自己 squash 进 main。** 不要让 Charles 盯 CI。
2. 告诉 Charles「可以 pull」的时机：是 **`main` HEAD CI 全绿**，不是 PR 页刚绿。
3. **不要移动**标签 `v0.9.0`、`v1.0.0`、`v1.1.0`，以及更早的 `v0.x.0`。
4. 不要恢复「基本信息」页，不要把 planning-run 加回操作者导航。
5. 操作者 KEY **恰好五个**，schema `OperatorProcessInputV1` **1.1.0**。
6. 计算器身份冻结：`cold_room_zone_plan@1.0.0`（**不要 bump `VERSION`**）。报告类型身份冻结：`cold_storage_concept_design@1.0.0`。
7. 飞书自定义 MCP：**传输方式 Streamable HTTP**；请求地址 `{ORIGIN}/api/v1/aily/v1/mcp/sse`；不要 GET SSE；隧道指 `:8000` 不是 Vite `:5173`。
8. 未得到 Charles **可以派发 / 授权** 前：不要做冷负荷/设备/装机功率/投资公式重切，不要系数晋级，不要生产部署。
9. 对 Charles 只讲用户能感知的结果。给飞书步骤时用**一个** `text` 代码块。

---

## 4. V1.1 已交付

| 包 | 内容 | PR |
|---|---|---|
| P0/P1 | REST `POST /api/v1/aily/v1/zone-plan` | #237 |
| P2 | 豆包技能包 | #238 |
| P3 | 可选 `X-Aily-Connector-Key` | #240 |
| P4 | 操作手册 | #239 |
| P5 | MCP Streamable HTTP，工具 `preview_zone_plan` | #243 |

吨 = 每天。豆包 owns NLP。本系统不解析聊天。

---

## 5. 诚实缺口（Release 已写）

- 出站飞书会话仍关。
- 冷量/设备/功率/投资公式 recut 未授权。
- GET SSE 在 trycloudflare / 飞书云上不可用。
- 工作台债务 TD-008 / TD-019 / TD-023 仍在。

Issues **#11 / #13 / #17 / #176 / #20 stay CLOSED**。

---

## 6. 下一会话怎么开始

```text
先读 docs/handover/2026-08-29-v1.1.md 和 AGENTS.md。
当前产品是 v1.1.0，标签钉在 7fd0a28。不要移动 v0.9.0 / v1.0.0 / v1.1.0。
对 Charles 说中文。红了自己修，绿了自己 squash 进 main，等主干 CI 绿再让他 git pull。
飞书 MCP 必须 Streamable HTTP，不要教 GET SSE。
未可以派发前不要做冷负荷/设备/功率/投资公式重切，不要直播 Aily 出站，不要施工图。
操作者只有五个 KEY；cold_room_zone_plan@1.0.0 不要 bump。
现在没有已派发的下一包。等 Charles 说下一步做什么。
```
